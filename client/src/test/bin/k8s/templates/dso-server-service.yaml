kind: Service
apiVersion: v1
metadata:
  name: "helloworldservice"
spec:
  selector:
    app: "hello-world"
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 80
    nodePort: 30001
  type: LoadBalancer

